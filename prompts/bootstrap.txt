Voce esta iniciando uma nova sessao neste projeto.

Objetivo desta etapa:
3) Seed de papéis/permissões iniciais e teste e2e 403

Pertence a: Module 8 — Qualidade
Título: Matriz mínima e testes de autorização
Descrição: Seed `admin`, `manager`, `operator`, `viewer` (RN08) com permissões exemplo; teste e2e provando que usuário sem `user:write` não acessa escrita.
Critérios de aceitação:

- seed reproduzível
- e2e cobre 403 em caso realista
- documentação dos `permission.code` iniciais
Prioridade: High
Estimativa: 3 SP
Label: seed, e2e, rbac, sprint-4

Passos obrigatorios:
1) Localize e leia o documento normativo principal do produto.
   - Neste repositorio, trate `PRD.md` como fonte normativa atual (visao, escopo, arquitetura e regras).
2) Leia `Sprints.md` como fonte oficial de backlog, execucao e priorizacao.
3) Leia `docs/CONTEXT.md` para recuperar a memoria curta da ultima sessao e `docs/PRD_DEVELOPMENT.md` para obter o historico consolidado do que foi desenvolvido.
4) Analise a estrutura atual do repositorio e stack real implementada.
5) Identifique o item de sprint mais aderente para a continuidade (sem inventar backlog).
6) Gere um resumo de impacto tecnico contendo:
   - item alvo;
   - arquivos/modulos que devem ser tocados;
   - riscos e dependencias;
   - divergencias entre PRD, backlog e codigo atual (se houver).
7) Pare e aguarde confirmacao antes de qualquer codificacao.

Regras:
- `PRD.md` = contrato normativo do produto.
- `Sprints.md` = contrato oficial de execucao do backlog.
- Nao mover itens de sprint.
- Nao inventar tarefas novas.
- Nao assumir implementacao pronta sem validar no codigo.
